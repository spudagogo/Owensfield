import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Communications"
      note="Official record only (threaded timeline). Managed by RG; members read-only; closed threads are archived."
    />
  );
}
