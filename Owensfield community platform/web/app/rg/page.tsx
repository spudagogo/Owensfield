import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Representative Group (RG)"
      note="RG tools are inaccessible to inactive users. Role-based access only; no implicit admin powers."
    />
  );
}
