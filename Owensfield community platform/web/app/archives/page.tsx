import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ArchivesPage() {
  return (
    <PlaceholderPage
      title="Archives"
      note="Archive-only access point for read-only records (governance items, minutes, documents)."
    />
  );
}

