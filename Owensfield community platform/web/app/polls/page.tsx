import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Polls"
      note="Governance changes require polls. Lifecycle: Draft → Pending RG Approval → Active → Closed → Archived."
    />
  );
}
