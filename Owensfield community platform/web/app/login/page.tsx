import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Login"
      note="Auth UI is not implemented yet. This route exists so middleware can redirect unauthenticated users here once Supabase Auth is configured."
    />
  );
}

